//
//  AppDelegate.swift
//  SoundApp
//
//  Created by Lander Wells on 5/24/23.
//

/*
 
 import SwiftUI
 
 class AppDelegate: NSObject, NSApplicationDelegate {
 static var popover = NSPopover()
 var statusBar: StatusBarController?
 
 func applicationDidFinishLaunching(_ notification: Notification) {
 Self.popover.contentViewController = NSHostingController(rootView: PopoverView())
 
 Self.popover.behavior = .transient
 Self.popover.contentSize = NSSize(width: 300, height: 400) // Set the initial size of the popover content
 
 
 statusBar = StatusBarController(Self.popover)
 
 // Set the origin of the window to a specific location
 let window = Self.popover.contentViewController?.view.window
 let windowOrigin = NSPoint(x: 100, y: 100) // Specify the desired coordinates
 window?.setFrameOrigin(windowOrigin)
 }
 
 
 }

*/
